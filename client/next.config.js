/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com', 'randomuser.me', 'images.unsplash.com', 'localhost'],
  }
}

module.exports = nextConfig;
